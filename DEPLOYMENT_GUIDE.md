# 🚀 Clover 정식 오픈 및 프로덕션 배포 가이드 (DEPLOYMENT_GUIDE.md)

본 가이드는 클로버(Clover)를 불특정 다수의 대중에게 공식 오픈할 때 필요한 **클라우드 데이터베이스 설정(동시성 확보)**, **카카오 디벨로퍼스 비즈 앱 전환**, 그리고 **개인정보처리방침 연동 절차**를 정리한 매뉴얼입니다.

---

## 📌 목차
1. [클라우드 PostgreSQL 데이터베이스 개설 (3분 완성)](#1-클라우드-postgresql-데이터베이스-개설-3분-완성)
2. [카카오 디벨로퍼스 설정 (비즈 앱 전환 및 약관 등록)](#2-카카오-디벨로퍼스-설정-비즈-앱-전환-및-약관-등록)
3. [고객지원 채널 및 운영자 정보 수정](#3-고객지원-채널-및-운영자-정보-수정)
4. [보안 및 자동 자정작용 확인](#4-보안-및-자동-자정작용-확인)

---

## 1. 클라우드 PostgreSQL 데이터베이스 개설 (3분 완성)

현재 로컬 개발 환경에서는 단일 파일 SQLite(`backend/dev.db`)를 사용하고 있습니다. 사용자가 수백~수천 명으로 늘어났을 때의 파일 락(DB 잠김)을 방지하기 위해 무료 클라우드 PostgreSQL(Supabase 또는 Neon)로 전환할 수 있습니다.

### Step 1: 무료 DB 프로젝트 생성 (Supabase 추천)
1. **[Supabase(supabase.com)](https://supabase.com)** 에 접속하여 무료 회원가입 후 로그인합니다.
2. **`New Project`**를 클릭하고 프로젝트 이름(예: `clover-db`)과 데이터베이스 암호를 입력합니다.
3. 리전(Region)은 **`Seoul (ap-northeast-2)`**을 선택합니다.
4. 생성이 완료되면 좌측 메뉴의 **Project Settings > Database**로 이동합니다.
5. **Connection String > URI** 탭에서 주소를 복사합니다:
   ```env
   postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require
   ```

### Step 2: 클로버 프로젝트 DB 프로바이더를 PostgreSQL로 전환
터미널에서 `backend` 폴더로 이동 후 제공된 스위칭 유틸리티를 실행합니다:
```bash
cd backend
node scripts/switch-db.js postgresql
```
*(언제든 로컬 개발/E2E 테스트로 되돌아가려면 `node scripts/switch-db.js sqlite`를 실행하시면 됩니다.)*

### Step 3: 클라우드 DB에 테이블 생성
`.env` 파일(또는 배포 호스팅 서버 환경변수)의 `DATABASE_URL`에 방금 복사한 Supabase 주소를 입력한 후 다음 명령어를 실행합니다:
```bash
npx prisma db push
```
*(Supabase 콘솔의 Table Editor에서 유저, 모임, 일정, 투표 테이블이 자동으로 모두 깔끔하게 생성된 것을 확인하실 수 있습니다.)*

---

## 2. 카카오 디벨로퍼스 설정 (비즈 앱 전환 및 약관 등록)

정식 오픈 시 일반 사용자들이 카카오 로그인 및 알림톡을 원활하게 받으려면 카카오 개발자 콘솔에서 다음 2가지를 완료해야 합니다:

### 1) 개인정보처리방침 URL 등록 (필수)
1. **[카카오 디벨로퍼스 콘솔](https://developers.kakao.com)** 에 접속하여 클로버 앱을 선택합니다.
2. **앱 설정 > 기본 정보** 메뉴로 이동합니다.
3. **개인정보처리방침 URL** 항목에 다음 링크를 입력하고 저장합니다:
   ```text
   https://clover-gilt.vercel.app/privacy
   ```

### 2) 비즈 앱(비즈니스 앱) 전환 신청
* 개인 개발자 앱 상태에서는 카카오 친구 목록 권한이나 알림톡 발송 인원에 제한이 있습니다.
* 카카오 디벨로퍼스 좌측 **비즈니스 > 비즈 앱 정보**에서 **`비즈 앱으로 전환`**을 클릭하고 간단한 사업자/개인 인증을 진행하시면 일반 모든 사용자에게 제한 없이 카카오 알림톡 및 친구 기능이 확장됩니다.

---

## 3. 고객지원 채널 및 운영자 정보 수정

약관 및 개인정보처리방침 화면(`Terms.tsx`, `Privacy.tsx`)에 표기된 고객지원 이메일을 실제 운영용 이메일이나 카카오톡 오픈채팅방 링크로 교체하실 수 있습니다:

* **파일 위치**:
  * `frontend/src/pages/Terms.tsx` (하단 문의처)
  * `frontend/src/pages/Privacy.tsx` (하단 개인정보 보호책임자)
* **현재 기본값**: `official.clover.team@gmail.com`
* 원하시는 대표 이메일 또는 카카오 오픈채팅 링크(`https://open.kakao.com/o/...`)로 텍스트를 수정하시면 됩니다.

---

## 4. 보안 및 자동 자정작용 확인

이번 업데이트를 통해 다음 보안/운영 기능이 서비스에 기본 내장되었습니다:

* **🚨 부적절 콘텐츠 신고 기능**: 모임 화면 상단 및 각 게시글에 [신고] 버튼이 활성화되어 음란물, 사기, 욕설 게시물을 사용자가 즉시 접수할 수 있으며, 관리자 콘솔(`/admin/feedback`)에서 실시간으로 확인 및 차단할 수 있습니다.
* **🚪 원클릭 회원 탈퇴**: 마이페이지 및 설정 화면에서 사용자가 언제든 본인 계정 및 개인정보를 영구 파기할 수 있어 개인정보보호법 및 앱 스토어 심사 기준을 100% 충족합니다.
* **🛡️ 안티 디도스/도배 방지 (Rate Limiting)**: 악의적인 봇이나 매크로가 과도한 요청을 보낼 경우 IP별로 분당 요청 수가 안전하게 제한됩니다.
